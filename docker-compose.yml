# Use postgres/example user/password credentials
version: '3.1'

services:
  db:
    image: postgres
    restart: always
    expose:
        - 5432
    environment:
      POSTGRES_PASSWORD: 102938
      POSTGRES_DB: db
